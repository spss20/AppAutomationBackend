{
  "routes": [
    {
      "method": "GET",
      "path": "/allinones",
      "handler": "allinone.find",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/allinones/count",
      "handler": "allinone.count",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/allinones/:id",
      "handler": "allinone.findOne",
      "config": {
        "policies": []
      }
    },
    {
      "method": "POST",
      "path": "/allinones",
      "handler": "allinone.create",
      "config": {
        "policies": []
      }
    },
    {
      "method": "PUT",
      "path": "/allinones/:id",
      "handler": "allinone.update",
      "config": {
        "policies": []
      }
    },
    {
      "method": "DELETE",
      "path": "/allinones/:id",
      "handler": "allinone.delete",
      "config": {
        "policies": []
      }
    }
  ]
}
